import 'package:flutter/material.dart';

class IzinCutiScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Izin / Cuti'),
      ),
      body: Center(
        child: Text('Halaman Izin dan Cuti'),
      ),
    );
  }
}
