import 'package:flutter/material.dart';

class DataAdminScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Absensi'),
      ),
      body: Center(
        child: Text('Halaman Absensi Karyawan'),
      ),
    );
  }
}
