import 'package:flutter/material.dart';

class DataKaryawanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Data Karyawan'),
      ),
      body: Center(
        child: Text('Halaman Data Karyawan'),
      ),
    );
  }
}
