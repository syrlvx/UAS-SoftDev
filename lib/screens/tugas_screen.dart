import 'package:flutter/material.dart';

class TugasScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tugas'),
      ),
      body: Center(
        child: Text('Halaman Tugas Karyawan'),
      ),
    );
  }
}
